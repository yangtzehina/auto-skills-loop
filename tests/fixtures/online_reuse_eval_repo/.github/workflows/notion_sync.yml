name: notion-sync

on:
  workflow_dispatch:
  push:
    paths:
      - "docs/**"
      - "templates/**"
      - "config/notion_schema.json"

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/publish_decision.py
